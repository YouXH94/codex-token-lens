# 只读边界与独立运行

**本版本无需修改 Codex 的文件、配置或进程。**所有新增能力均在 Lens 自身前后端及启动器中实现。

## 访问矩阵

| 对象 | 访问 |
|---|---|
| Codex sessions / archived_sessions JSONL | 只读扫描、元数据检查；不创建/追加/删除/重命名 |
| Codex auth.json、config.toml、skills、hooks、AGENTS.md | 正常运行不读取内容，不写入；不注册 MCP/OTel |
| Lens 安装目录 | 读取静态前端、代码、费率；部署时只更新本工具文件 |
| `~/.codex-token-lens/` | 本工具 SQLite、辅助文件、状态和运行日志；默认仅此状态目录可写 |
| 显式指定的 `--db` / `--state-dir` / `--export` | 用户指定的本工具目标，经路径检查后写入；不能在受保护来源内 |
| 浏览器本地存储 | 别名与显示偏好；清除站点数据即可移除 |
| 浏览器下载位置 | 用户主动导出；由浏览器控制，不能由 Python 强制校验目标目录 |
| 网络 | 只在 `127.0.0.1` 服务/管理自身；不发外部模型、账户、价格、遥测请求 |
| 进程 | 启动/停止 Lens 自身；不启动/控制/重启 Codex |

扫描 JSONL 必须读取文件字节；只持久化白名单内的用量、时间、模型/档位、路径与归属、配额快照等元数据。**不持久化或上传提示词、代码正文、工具结果**。数据库仍含使用习惯、工程路径等隐私信息，应当作为私人文件保存。

## 误写防护

受保护路径包括默认 `~/.codex`、环境变量 `CODEX_HOME`、显式来源及 sessions/archived_sessions 的父级。写入前解析真实路径；拒绝受保护目录内的数据库/状态/导出/安装路径，以及现有符号链接或多重硬链接文件目标；SQLite 辅助文件也检查。

这是用于防止正常部署错误的边界检查，**不是抵御同一用户恶意竞争改链的操作系统沙箱**。读取文件可能由操作系统更新访问时间 atime；测试保证的是原始字节与修改时间 mtime 不变。浏览器的另存为窗口不受后端控制，导出时请不要选择 Codex 目录。

## 本地网页安全

监听地址固定 `127.0.0.1`，不提供公网/局域网绑定选项。数据接口要求随机授权密钥对应的 HttpOnly、SameSite=Strict Cookie；校验 Host。静态文件只允许明确白名单，拒绝任意路径读取。数据接口没有写原始日志/修改配置的能力。

`POST /api/shutdown` 是唯一的自身生命周期操作，要求额外 Bearer 密钥；仅拥有浏览器 Cookie 不能调用它。它不执行系统命令或强制终止其他 PID。服务管理器先认证实例，再停止该实例；不会对过期 PID 随意发 kill。

`runtime.json` 和 `service.log` 可能含授权地址/密钥，不要分享。状态目录/新文件在支持 POSIX 权限的平台设置为 700/600；Windows 权限最终由当前用户目录 ACL 决定，不宣称 Windows 已完成权限加固审计。浏览器在自己的站点数据中存别名；同机同账户程序不是此工具的防护边界。

## 卸载和后续变更

先执行 `service.py stop`，再删除独立安装目录；自己的状态目录是否保留由用户决定。无需回滚 Codex 文件。不得清理 Codex 原始日志来卸载本工具。

以后若需开机自启、系统托盘原生安装、远程访问或改变 Codex 日志设置，必须单独说明改动对象并取得确认。本包没有自动实施这些变更。
