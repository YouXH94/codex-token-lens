# 给本地 Codex 的部署提示词

将下文直接交给本地 Codex。仅部署与验收监控软件；不要通过模型对话查询日常用量。

---

请将当前解压目录中的 **Codex Token Lens v0.2.0** 部署为独立本地应用。先阅读 `README.md`、`SECURITY.md`、`TEST_RESULTS.md` 与 `service.py`，再执行。完成后给出已执行步骤和实际验收结果，不只输出安装建议。

## 不可突破的边界

1. **不修改 Codex 的任何文件或运行设置**：不得写入 `~/.codex`、`CODEX_HOME`、指定日志来源，以及传入 sessions/archived_sessions 的父级 Codex 目录。不得编辑 config.toml、auth.json、原始会话日志、AGENTS.md、skills、hooks、MCP、OTel 或沙箱设置，不运行 `codex mcp add`，不重装/升级/重启 Codex，不设置 API 代理，不读取登录凭据。
2. 只允许安装/修改本工具自己的项目目录，以及默认 `~/.codex-token-lens/` 状态目录。不能将本工具安装到任何 Codex 目录内。不要覆盖其他项目或删除既有数据。
3. **若遇到确实需要 Codex 文件改动、管理员权限、安全权限扩大、系统服务/自启/防火墙/代理/shell 配置改动，立刻停在该步骤**，列出“具体路径或设置、原因、影响、无需改动的替代方案”，等待我确认；不得自行批准。本方案正常运行不需要上述改动。
4. 不调用模型/API 生成测试用量，不为验收创建 Codex 对话，不询问 Codex 自己用了多少 token。用包内合成日志测试与已有只读日志验收即可。
5. 不把任何原始会话内容、数据库、路径全量清单、runtime.json、service.log、授权 URL 密钥上传到远程服务或贴进最终报告。显示正常程序路径、端口、PID 和概括性覆盖结果即可。

## 执行顺序

### A. 检查环境和目录

- 确认当前解压包确实包含 lens.py、service.py、static/、rates.json。
- 确认独立安装路径；可在空闲的用户应用目录使用 `codex-token-lens`。若当前位置已是安全的独立目录，无需另建副本。若同名目录有旧内容，先检查，不直接覆盖。
- 确认 Python 3.10+。macOS/Linux 用 `python3`；Windows 优先 `py -3`。运行仅需标准库；不要执行 pip/npm 安装。缺少 Python 时报告并询问是否安装，不自行进行全局环境变更。
- 从环境变量 `CODEX_HOME` 和标准目录存在性确定日志来源；不读取 auth.json/config.toml。需要非标准来源时使用 Lens 的 `--home`，绝不修改 Codex 配置。
- 确认默认状态目录 `~/.codex-token-lens/` 不在受保护路径内。可读取自己的旧 runtime.json 检查 Lens 是否已运行，但不将其中密钥发给第三方。

### B. 部署并启动

- 如有已认证的 0.2.x Lens 实例，使用它自己的 `service.py stop` 停止后更新；绝不 `killall python` 或按模糊名称杀进程。遇到其他版本/无法认证的实例，报告冲突，改端口或等待我确认如何处理。
- 若已有 Lens 数据库，先停止 Lens，再备份**工具自己的**状态目录；不要碰 Codex 数据。保留浏览器别名不等于修改原始会话标题。
- 在项目目录运行：
  - macOS/Linux：`python3 -B service.py start`
  - Windows：`py -3 -B service.py start`
- 需要自定义来源时追加一个或多个 `--home "实际路径"`。端口被占用时可改用 `--port 8766`。现有实例不会自动应用新参数；应先安全停止再重启。
- `start` 会产生独立后台进程；让用户本地浏览器打开它输出的授权地址。无桌面环境用 `--no-browser`，随后提供本机打开方法，不开放到局域网/公网。
- 如果 Codex 工具宿主限制持久进程或会清理整个沙箱，把同一个启动命令交给我在系统普通终端执行；不要为了常驻而自行扩大 Codex 权限或安装系统服务。
- 不安装自启或操作系统服务。浏览器打不开时使用 `service.py open`，不要降低安全检查或删除 Cookie 校验。

### C. 实际验收

- 执行 `python3 -B -m unittest discover -s tests -p 'test_*.py' -v`（Windows 替换 Python 命令）。符号链接权限不足导致的跳过应如实记录，不能记为通过。
- Node/Playwright 是可选开发工具；环境已具备可运行相应测试，没有就跳过并说明，不全局安装依赖。
- 使用 `service.py status` 确认独立 PID、健康状态、回环端口。关闭启动终端后，再从新终端检查 PID 仍在线。
- 在**这台机器的真实浏览器**验收：授权地址→页面静态资源→真实日志报表→刷新保持登录；不允许只拿 preview.html 冒充部署成功。检查每日/周期/Threads、日期小时下钻、中文单位、筛选排序、右侧详情、精确值、导出。
- 测试一个无敏感信息的面板别名，刷新验证它保存在浏览器且没有写入 Codex；清理仅此测试别名。
- 检查报告对未知字段/价格/来源缺失有明确提示。无日志或无 usage 时显示真实空状态，不凭空补数据。
- 用临时合成日志验证采集前后源文件字节与修改时间保持不变；不要要求正在运行的 Codex 的真实日志全局哈希不变，因为 Codex 自己会正常追加。不得停止 Codex 来满足这个检查。
- 验证 `service.py stop` 只停止 Lens，再次 `start` 能复用自己的数据库。验收结束保持 Lens 正常运行，除非我要求停止。

### D. 交付结果

简要列出：安装目录、Python 版本、独立 PID/端口、自己的数据目录、已覆盖的日志来源数量与时间范围、实际测试通过/跳过/未验证项、启动/打开/停止命令。

单独明确说明：**是否改动 Codex 文件，是否添加自启/系统服务，是否访问模型 API**。正常答案均为“否”。遇到日志格式差异时，仅修改本工具解析器并补充脱敏合成回归测试；任何 Codex 侧改动必须先请求我的确认。

不要给出未经测量的 CPU/内存数字，也不要把 Credits 估算当成官方账单。
